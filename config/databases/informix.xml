<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE database PUBLIC "-//MMBase//DTD database config 1.2//EN" "http://www.mmbase.org/dtd/database_1_2.dtd">
<database>

		<!-- <name>
				The Name of the Database we are running, only used for display
		-->
		<name>Informix</name>

		<!-- <mmbasedriver>
				Defines the database driver that handles the abstraction between
				mmbase and the given jdbs/database we are using.
		-->
		<mmbasedriver>org.mmbase.module.database.support.MMInformix42Node</mmbasedriver>

		<!-- <sqlhandler>
				Defines the SqlHandler that converts SearchQuery objects to sql statements.
		-->
		<sqlhandler>org.mmbase.storage.search.implementation.database.InformixSqlHandler</sqlhandler>

		<!-- <chainedsqlhandler>
				Defines the chained SqlHandler that extend the sqlhandler's functionality.
				This enables database extensions to be used.
		-->
		<chainedsqlhandler></chainedsqlhandler>

		<!-- <scheme>
				Defines the strings for the different scheme database
				statements.
		-->
		<scheme>
				<create>create table</create>
				<not-null>not null</not-null>
				<null>null</null>
				<primary-key>primary key</primary-key>
		</scheme>

		<!-- <mapping>
				defines how mmbase should map the different types mmbase to this
				database.

				mmbase-type, the internal mmbase datatypes allways in caps
				min-size, minimal size this value is valid for
				max-size, maximum size this value is valid for
		-->
		<mapping>
			 <type-mapping mmbase-type="INTEGER">integer</type-mapping>
			 <type-mapping mmbase-type="NODE">integer</type-mapping>
			 <type-mapping mmbase-type="LONG">int8</type-mapping>
			 <type-mapping mmbase-type="FLOAT">float(size)</type-mapping>
			 <type-mapping mmbase-type="STRING" min-size="1" max-size="255">nvarchar(size)</type-mapping>
			 <type-mapping mmbase-type="STRING" min-size="256" max-size="32776">nchar(size)</type-mapping>
			 <type-mapping mmbase-type="STRING" min-size="32777" max-size="16577216">clob</type-mapping>
			 <type-mapping mmbase-type="XML" min-size="1" max-size="255">nvarchar(size)</type-mapping>
			 <type-mapping mmbase-type="XML" min-size="256" max-size="32776">nchar(size)</type-mapping>
			 <type-mapping mmbase-type="XML" min-size="32777" max-size="16577216">clob</type-mapping>
			 <type-mapping mmbase-type="BYTE" min-size="1" max-size="16777216">BLOB</type-mapping>
		</mapping>

		<!-- <disallowed>
				defines disallowed fields, tables etc etc for this database
				if possible it also explains to mmbase what todo with it.
				illegal fieldnames for example will be automapped to a
				replacement if one is defined. This way apps made for mmbase
				don't need to be changes when running on a different database.
		-->
		<disallowed>
					<field name="parent" replacement="prnt" />
					<field name="type" replacement="typ" />
					<field name="database" replacement="dtbs" />
					<field name="data" replacement="dt" />
		</disallowed>
</database>
