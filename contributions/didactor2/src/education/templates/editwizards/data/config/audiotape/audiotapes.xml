<?xml version="1.0"?>
<!DOCTYPE wizard-schema PUBLIC "-//MMBase/DTD editwizard 1.0//EN" "http://www.mmbase.org/dtd/wizard-schema_1_0.dtd"
[
   <!ENTITY extra_loads SYSTEM "extra_loads.xml">
   <!ENTITY extra_steps SYSTEM "extra_steps.xml">
   <!ENTITY extra_schemas SYSTEM "extra_schemas.xml">
]>
<wizard-schema id="empty">
    <title>Audiofragment editor</title>

    <action type="create">
        <object type="audiotapes">
            <field name="showtitle">1</field>
        </object>
    </action>
    
    <action type="load">
      &extra_loads;
    </action>

    <action type="delete"/>

    <lists>
       <optionlist include="config/option_list/boolean.xml" />
    </lists>

    <steps>
        <step form-schema="step1" />
        &extra_steps;
    </steps>

    <form-schema id="step1">
        <title>Invoeren audiofragment</title>
        <field name="title" dttype="string" dtminlength="2" >
            <prompt>Titel</prompt>
        </field>
        <field name="showtitle" ftype="enum">
           <prompt xml:lang="nl">Titel laten zien</prompt>
           <prompt xml:lang="en">Show title</prompt>
           <optionlist select="boolean" />
        </field>

        <field name="subtitle" dttype="string">
            <prompt>Subtitel</prompt>
        </field>

        <field name="intro" ftype="text" rows="4" >
            <prompt>Intro</prompt>
        </field>
        

        <field name="body" ftype="text" rows="8" >
            <prompt>Tekst</prompt>
        </field>

        <field name="url" ftype="line" dttype="string" dtminlength="2">
            <prompt>URL</prompt>
            <description>Verwijst naar een audio-fragment</description>
        </field>

    </form-schema>

     &extra_schemas;
</wizard-schema>
