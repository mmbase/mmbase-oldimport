<?xml version="1.0"?>
<!DOCTYPE wizard-schema PUBLIC "-//MMBase/DTD editwizard 1.0//EN" "http://www.mmbase.org/dtd/wizard-schema_1_0.dtd"
[
   <!ENTITY extra_loads SYSTEM "extra_loads.xml">
   <!ENTITY extra_steps SYSTEM "extra_steps.xml">
   <!ENTITY extra_schemas SYSTEM "extra_schemas.xml">
]>
<wizard-schema id="empty">
    <title>Link</title>

    <action type="create">
        <object type="urls">
            <field name="showtitle">1</field>
            <field name="url">http://</field>
        </object>
    </action>

    <action type="load">
      &extra_loads;
    </action>

    <action type="delete" />

    <lists>
       <optionlist include="config/option_list/boolean.xml" />
    </lists>

    <steps>
        <step form-schema="step1" />
        &extra_steps;
    </steps>

    <form-schema id="step1">
        <title>Link invoeren</title>
        <!--
        <description>Invoeren van links (URLs)</description>
        -->

        <field name="url" >
            <prompt>Link</prompt>
            <description>Voer hier een nieuwe link (URL) in of wijzig een bestaande.</description>
        </field>

        <field name="name" >
            <prompt>Naam van de link</prompt>
            <description>Voer hier een naam in voor deze link of wijzig een bestaande. De naam wordt gebruikt om een link af te beelden.</description>
        </field>
        <field name="showtitle" ftype="enum">
           <prompt xml:lang="nl">Titel laten zien</prompt>
           <prompt xml:lang="en">Show title</prompt>
           <optionlist select="boolean" />
        </field>

        <field name="description" ftype="text" rows="4" >
            <prompt>Omschrijving</prompt>
            <description>Geef een duidelijke omschrijving van de link</description>
        </field>
    </form-schema>

    &extra_schemas;
</wizard-schema>
