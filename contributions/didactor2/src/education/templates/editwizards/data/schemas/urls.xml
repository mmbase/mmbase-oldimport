<?xml version="1.0"?>
<!DOCTYPE form-schema PUBLIC "-//MMBase/DTD editwizard 1.0//EN" "http://www.mmbase.org/dtd/wizard-schema_1_0.dtd">

    <form-schema>
        <title>URLs</title>
     <list role="related" extends="list/urls.xml" />
 
        <!-- we need a new list because there can be only one search command per list -->
        <list minoccurs="0" maxoccurs="*" role="related">
            <title>Zoeken op tekenreeks</title>
            <item displaytype="none"></item>
 
            <action type="create">
                <relation role="related" destinationtype="urls"/>
            </action>


             <command name="search" nodepath="metalangstring,metadata,urls" fields="urls.name,metalangstring.value" age="7" orderby="urls.number" directions="down" >
                <prompt>Zoeken op metadata</prompt>
                <search-filter>
                    <name>Metadata tekenreeks bevat</name>
                    <search-fields>metalangstring.value</search-fields>
                </search-filter>
           </command>           
        </list>

        <!-- we need a new list because there can be only one search command per list -->
        <list minoccurs="0" maxoccurs="*" role="related">
            <title>Zoeken op vocabulair</title>
            <item displaytype="none"></item>
 
            <action type="create">
                <relation role="related" destinationtype="urls"/>
            </action>


             <command name="search" nodepath="metavocabulary,metadata,urls" fields="urls.name,metavocabulary.value" age="7" orderby="urls.number" directions="down" >
                <prompt>Zoeken op metadata</prompt>
                <search-filter>
                    <name>Metadata vocabulair bevat</name>
                    <search-fields>metavocabulary.value</search-fields>
                </search-filter>
           </command>
           
        </list>

        <!-- we need a new list because there can be only one search command per list -->
        <list minoccurs="0" maxoccurs="*" ordertype="number" role="related">
            <title>Zoeken op datum (YYYY-MM-DD HH:MM:SS)</title>
            <item displaytype="none"></item>
 
            <action type="create">
                <relation role="posrel" destinationtype="urls"/>
            </action>


             <command name="search" nodepath="metadate,metadata,urls" fields="urls.name,metadate.value" age="7" orderby="urls.number" directions="down" >
                <prompt>Zoeken naar leerobjecten op metadata</prompt>
                <search-filter>
                    <name>Metadata datum bevat</name>
                    <search-fields>FROM_UNIXTIME(metadate.value)</search-fields>
                </search-filter>
           </command>
           
        </list>
 


    </form-schema>
   


