<!DOCTYPE list PUBLIC "-//MMBase/DTD editwizard 1.0//EN" "http://www.mmbase.org/dtd/wizard-schema_1_0.dtd">
<list destination="audiotapes" role="posrel" minoccurs="0" maxoccurs="*" ordertype="number" orderby="field[@name='pos']">
    <title>Bijbehorende Audio</title>

    <item>
        <field name="title" ftype="data">
            <prompt>Titel</prompt>
        </field>
        <field ftype="data" name="intro">
            <prompt>Omschrijving</prompt>
        </field>
       <field wizardname="audiotapes" ftype="startwizard" />
   </item>

    <command name="startwizard" inline="true" wizardname="audiotapes" objectnumber="new" />

    <command name="search" nodepath="audiotapes" fields="title" age="-1" orderby="number" directions="down" >
        <prompt>Zoeken naar audio</prompt>
        <search-filter>
            <name>Titel bevat</name>
            <search-fields>title</search-fields>
        </search-filter>
        <search-filter>
            <name>Omschrijving bevat</name>
            <search-fields>intro</search-fields>
        </search-filter>
    </command>

    <action type="create">
        <relation destinationtype="audiotapes" role="posrel">
          <field name="pos">{sum(//relation[@role='posrel' and @lastitem='true']/field[@name='pos'])+{$pos}}</field>
        </relation>
   </action>
</list>
