package com.finalist.cmsc.tools.generator;

import junit.framework.TestCase;

import java.util.List;

import com.finalist.cmsc.tools.generator.VCSConfig;

public class BatchOperationTaskTest extends TestCase {

    public void testGetConfigs() {
//        String configFileLocation = "/Users/gmark/temp/config";
//        BatchOperationTask task = new BatchOperationTask();
//        task.setConfigfile(configFileLocation);
//        List<VCSConfig> configs = task.getConfigs();
//
//        assertEquals(2, configs.size());
//        assertEquals(":pserver:guest@cvs.mmbase.org:/var/cvs", configs.get(0).getUrl());
//        assertEquals("guest", configs.get(0).getPassword());
//        assertEquals("contributions/CMSContainer", configs.get(0).getModule());
//        assertEquals("cvs", configs.get(0).getType());
//
//
//        assertEquals("https://extranet.finalist.com/svn/nai/trunk", configs.get(1).getUrl());
//        assertEquals("mark.guo", configs.get(1).getUsername());
//        assertEquals("whc873bg", configs.get(1).getPassword());
//        assertEquals("nai", configs.get(1).getModule());
//        assertEquals("svn", configs.get(1).getType());


//        System.out.println(configFileLocation);
    }
}
