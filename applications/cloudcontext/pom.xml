<?xml version="1.0"?>
<project>
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.mmbase</groupId>
    <artifactId>mmbase-application-parent</artifactId>
    <version>1.9</version>
    <relativePath>../app-base/pom.xml</relativePath>
  </parent>

  <artifactId>mmbase-cloudcontext</artifactId>
  <name>MMBase CloudContext Security</name>
  <version>${mmbase.version}</version>

  <description>
    The MMBase cloudcontext security module allows you to administrate your users in the database, and set very fine-grained security ranks on them.
  </description>

  <properties>
    <mmbaseComponentId>cloudcontext</mmbaseComponentId>
  </properties>

</project>
