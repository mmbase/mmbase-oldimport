<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mainFilter SYSTEM "http://www.mmbase.org/dtd/mediafilter_1_0.dtd" [
 <!ENTITY % configSubTags "preferredSource , realAudio">
 <!ENTITY % format SYSTEM "http://www.mmbase.org/dtd/formatcomparator_1_0.dtd" >
 <!ENTITY % real   SYSTEM "http://www.mmbase.org/dtd/realcomparator_1_0.dtd" >
 %format;
 %real;
]>
<mainFilter>
	<!-- 
  Define the chain of filters, the filter that has to filter first
  has to be mentioned as first. A filter is specified or by the classname
  (class needs to be ResponseInfoComparator).
		-->
	<chain>
		<filter>org.mmbase.util.media.AvailableComparator</filter>
		<filter id="clienformat">org.mmbase.util.media.ClientFormatComparator</filter>
		<filter id="format">org.mmbase.util.media.ServerFormatComparator</filter>
		<filter id="real">org.mmbase.util.media.RealComparator</filter>
	</chain>

  <filterConfigs>

    <config filter="format">
    <!-- 
    Configuration of ServerFormatComparator 
    Define the sequence of preference of the servers.
    -->
      <preferredSource>    
        <source format="wmp" />
        <source format="ram" />
        <source format="rm" />
        <source format="ra" />
        <source format="asf" />
      </preferredSource>
    </config>
    
    <!-- 
    Configuration of RealComparator
    Used to specify realaudio preferences.
    -->
    <config filter="real">
     <realAudio>
      <minspeed>16000</minspeed>
      <maxspeed>96000</maxspeed>
      <minchannels>1</minchannels>
      <maxchannels>2</maxchannels>
     </realAudio>
    </config>

  </filterConfigs>

</mainFilter>
