<?xml version="1.0"?>
<!DOCTYPE project
 [ <!ENTITY base     SYSTEM "buildbase.xml" > ]
>

<project name="buildbase" basedir=".">
  <dirname property="buildbase.dir" file="${ant.file.buildbase}"/>
  <property name="applications.dir"   location="${buildbase.dir}" />
  &base;

</project>